import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'column',
    flexWrap: 'wrap',
  },
  typeText: {
    backgroundColor: '#A3CD77',
    margin: 5,
    padding: 8,
    borderRadius: 12,
    fontSize: 14,
    color: '#333',
  },
});
